
version: "3.9"

services:
  ubuntu:
    container_name: workplanner-api
    build: server/
    # image: workplanner-api
    ports:
      - "8080:8080"
    volumes:
      - ./server/:/home/server

    